"use client";

import { useMemo, useState } from "react";

export interface TradeAnalyzerCatalogAsset {
  assetId: string;
  displayName: string;
  assetType: "PLAYER" | "PICK" | "K" | "DST" | "UNSUPPORTED";
  position?: string;
  ownerId?: string;
  ownerName?: string;
  marketValue?: number;
  evidence?: string;
  imageUrl?: string;
  nflTeam?: string;
  rosterStatus?: string;
}

export interface TradeAnalyzerTeam {
  ownerId: string;
  teamName: string;
  ownerName: string;
  imageUrl: string;
  draftCapital: ReadonlyArray<{ id: string; season: number; round: number; slot?: number; currentOwnerId: string; originalTeamName: string }>;
}

type Result = { ok: true; data: ApiData } | { ok: false; error?: { message?: string } };
type ApiData = { sideA: SideResult | null; sideB: SideResult | null; trade: TradeResult | null };
type SideResult = { assets: Array<{ assetId: string; displayName: string; baseValue?: number }>; rawValue: number; marketShare: { display: number } | null; evidence: string };
type TradeResult = { fairnessScore: { display: number } | null; fairnessBand: string | null; marketEdgeSide: string | null; evidence: string; resultStatus: string };

const positions = ["QB", "RB", "WR", "TE", "K", "DST"];
const money = (value: number) => value >= 1000 ? `${(value / 1000).toFixed(1)}K` : Math.round(value).toLocaleString();
const errorMessage = (status: number) => ({ 400: "One or more selected assets are unavailable.", 401: "Sign-in required.", 403: "Trade Analyzer is not available yet.", 413: "That request is too large.", 415: "Trade Analyzer requires JSON input.", 429: "Too many requests — try again shortly.", 500: "Unable to analyze this trade right now.", 503: "Trade Analyzer is temporarily unavailable." }[status] ?? "Unable to analyze this trade right now.");

export default function TradeAnalyzerOwnerClient({ catalog, teams, defaultOwnerId, snapshotDate }: { catalog: TradeAnalyzerCatalogAsset[]; teams: TradeAnalyzerTeam[]; defaultOwnerId: string; snapshotDate: string }) {
  const initialOwner = teams.some((team) => team.ownerId === defaultOwnerId) ? defaultOwnerId : "";
  const [mode, setMode] = useState<"LEAGUE" | "SANDBOX">("LEAGUE");
  const [teamA, setTeamA] = useState(initialOwner), [teamB, setTeamB] = useState("");
  const [packageA, setPackageA] = useState<string[]>([]), [packageB, setPackageB] = useState<string[]>([]);
  const [query, setQuery] = useState(""), [position, setPosition] = useState("ALL"), [group, setGroup] = useState<"PLAYERS" | "PICKS">("PLAYERS");
  const [result, setResult] = useState<ApiData | null>(null), [error, setError] = useState(""), [loading, setLoading] = useState(false);
  const selected = useMemo(() => new Set([...packageA, ...packageB]), [packageA, packageB]);
  const assetsFor = (ownerId: string) => catalog.filter((asset) => asset.ownerId === ownerId);
  const visibleSandbox = useMemo(() => catalog.filter((asset) => (group === "PICKS" ? asset.assetType === "PICK" : asset.assetType !== "PICK") && (!query.trim() || `${asset.displayName} ${asset.ownerName ?? ""}`.toLowerCase().includes(query.trim().toLowerCase())) && (group === "PICKS" || position === "ALL" || asset.position === position)).slice(0, 80), [catalog, group, position, query]);
  const reset = (nextMode: "LEAGUE" | "SANDBOX" = "LEAGUE") => { setMode(nextMode); setTeamA(nextMode === "LEAGUE" ? initialOwner : ""); setTeamB(""); setPackageA([]); setPackageB([]); setResult(null); setError(""); setQuery(""); };
  const selectTeam = (side: "A" | "B", ownerId: string) => { if ((side === "A" && ownerId === teamB) || (side === "B" && ownerId === teamA)) return; if (side === "A") { setTeamA(ownerId); setPackageA([]); } else { setTeamB(ownerId); setPackageB([]); } setResult(null); };
  const add = (assetId: string, side: "A" | "B") => { if (selected.has(assetId)) return; const ownerId = side === "A" ? teamA : teamB; const allowed = mode === "SANDBOX" || catalog.find((asset) => asset.assetId === assetId)?.ownerId === ownerId; if (!allowed) return; const setter = side === "A" ? setPackageA : setPackageB; setter((items) => items.length < 15 ? [...items, assetId] : items); setResult(null); };
  const remove = (assetId: string, side: "A" | "B") => { (side === "A" ? setPackageA : setPackageB)((items) => items.filter((id) => id !== assetId)); setResult(null); };
  const analyze = async () => { if (!packageA.length || !packageB.length || loading) return; setLoading(true); setError(""); setResult(null); try { const response = await fetch("/api/trade-analyzer/analyze", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ sideA: { assetIds: packageA }, sideB: { assetIds: packageB } }) }); const payload = await response.json() as Result; if (!response.ok || !payload.ok) setError(payload.ok ? "Unable to analyze this trade right now." : payload.error?.message ?? errorMessage(response.status)); else setResult(payload.data); } catch { setError("Unable to analyze this trade right now."); } finally { setLoading(false); } };
  const labels = mode === "LEAGUE" ? [teams.find((team) => team.ownerId === teamA)?.teamName ?? "Participant 1", teams.find((team) => team.ownerId === teamB)?.teamName ?? "Participant 2"] : ["Side A", "Side B"];
  return <main className="lcc2-page-shell"><div className="lcc2-page-container"><header className="max-w-3xl"><p className="lcc2-label text-[var(--lcc-brand-primary)]">Private Dynasty Operations</p><h1 className="mt-2 font-ui text-4xl font-black tracking-[-0.04em] text-[var(--lcc-color-text)] sm:text-5xl">Trade Analyzer</h1><p className="lcc2-body mt-3">League Trade supports two-, three-, or four-team trades with market value, roster, and dynasty context. Trade Sandbox is asset-value-only and franchise-agnostic.</p></header><div className="mt-7 flex flex-wrap gap-2" role="tablist" aria-label="Trade Analyzer mode"><button type="button" role="tab" aria-selected={mode === "LEAGUE"} onClick={() => mode !== "LEAGUE" && reset("LEAGUE")} className={`lcc2-button ${mode === "LEAGUE" ? "lcc2-button--primary" : "lcc2-button--secondary"}`}>League Trade</button><button type="button" role="tab" aria-selected={mode === "SANDBOX"} onClick={() => mode !== "SANDBOX" && reset("SANDBOX")} className={`lcc2-button ${mode === "SANDBOX" ? "lcc2-button--primary" : "lcc2-button--secondary"}`}>Trade Sandbox</button></div><section className="mt-6 grid gap-4 lg:grid-cols-2" aria-label="Trade participants"><Participant label="Participant 1" team={teams.find((team) => team.ownerId === teamA)} teams={teams} selected={packageA} catalog={catalog} onTeamChange={(id) => selectTeam("A", id)} onRemove={(id) => remove(id, "A")} /><Participant label="Participant 2" team={teams.find((team) => team.ownerId === teamB)} teams={teams} selected={packageB} catalog={catalog} onTeamChange={(id) => selectTeam("B", id)} onRemove={(id) => remove(id, "B")} /></section><section className="lcc2-card mt-5 p-4 sm:p-5" aria-labelledby="assets-heading"><div className="flex flex-wrap items-end justify-between gap-3"><div><p className="lcc2-label">{mode === "LEAGUE" ? "Current franchise assets" : "Hypothetical asset catalog"}</p><h2 id="assets-heading" className="mt-1 font-ui text-2xl font-black text-[var(--lcc-color-text)]">{mode === "LEAGUE" ? "Select outgoing assets" : "Build a sandbox trade"}</h2></div><p className="lcc2-body text-sm">{mode === "LEAGUE" ? "Only assets owned by the selected franchise are selectable." : "Current ownership is informational only."}</p></div>{mode === "SANDBOX" ? <div className="mt-4 grid gap-3 sm:grid-cols-[minmax(0,1fr)_auto_auto]"><label className="grid gap-1"><span className="lcc2-label">Search roster and picks</span><input aria-label="Search roster and picks" value={query} onChange={(event) => setQuery(event.target.value)} className="lcc2-input" placeholder="Search by player, pick, or team" /></label><label className="grid gap-1"><span className="lcc2-label">Group</span><select aria-label="Sandbox asset group" value={group} onChange={(event) => setGroup(event.target.value as "PLAYERS" | "PICKS")} className="lcc2-input"><option value="PLAYERS">Players</option><option value="PICKS">Picks</option></select></label><label className="grid gap-1"><span className="lcc2-label">Position</span><select aria-label="Sandbox position" value={position} onChange={(event) => setPosition(event.target.value)} className="lcc2-input"><option>ALL</option>{positions.map((item) => <option key={item}>{item}</option>)}</select></label></div> : null}<div className="mt-4 grid gap-3 sm:grid-cols-2">{mode === "LEAGUE" ? <>{positions.map((item) => <RosterGroup key={item} position={item} assets={[...assetsFor(teamA), ...assetsFor(teamB)].filter((asset, index, all) => all.findIndex((candidate) => candidate.assetId === asset.assetId) === index && (asset.position === item || (item === "DST" && asset.assetType === "DST")))} selected={selected} add={add} teamA={teamA} />)}</> : visibleSandbox.map((asset) => <AssetRow key={asset.assetId} asset={asset} selected={selected.has(asset.assetId)} add={add} />)}</div></section><div className="mt-5 flex flex-wrap gap-3"><button type="button" onClick={analyze} disabled={!packageA.length || !packageB.length || loading} className="lcc2-button lcc2-button--primary disabled:cursor-not-allowed disabled:opacity-50">{loading ? "Analyzing…" : "Analyze Trade"}</button>{mode === "LEAGUE" ? <button type="button" onClick={() => { setTeamA(teamB); setTeamB(teamA); setPackageA(packageB); setPackageB(packageA); setResult(null); }} disabled={!teamA && !teamB} className="lcc2-button lcc2-button--secondary disabled:opacity-50">Swap Teams</button> : null}<button type="button" onClick={() => reset()} className="lcc2-button lcc2-button--secondary">Reset Trade</button></div><div className="mt-5" aria-live="polite">{error ? <div className="lcc2-card border-[var(--lcc-color-warning)] p-4"><p className="font-ui font-black text-[var(--lcc-color-text)]">{error}</p></div> : null}{result ? <ResultView result={result} labels={labels} catalog={catalog} snapshotDate={snapshotDate} /> : null}</div></div></main>;
}

function Participant({ label, team, teams, selected, catalog, onTeamChange, onRemove }: { label: string; team?: TradeAnalyzerTeam; teams: TradeAnalyzerTeam[]; selected: string[]; catalog: TradeAnalyzerCatalogAsset[]; onTeamChange: (id: string) => void; onRemove: (id: string) => void }) { return <article className="lcc2-card lcc2-card--raised p-4 sm:p-5"><p className="lcc2-label">{label}</p><label className="mt-2 grid gap-1"><span className="sr-only">{label} franchise</span><select aria-label={`${label} franchise`} value={team?.ownerId ?? ""} onChange={(event) => onTeamChange(event.target.value)} className="lcc2-input"><option value="">Choose a franchise</option>{teams.map((candidate) => <option key={candidate.ownerId} value={candidate.ownerId}>{candidate.teamName}</option>)}</select></label>{team ? <><div className="mt-4 flex items-center gap-3"><img src={team.imageUrl} alt="" className="h-10 w-10 rounded-full border border-[var(--lcc-color-border)] object-cover" /><div><h2 className="font-ui text-xl font-black text-[var(--lcc-color-text)]">{team.teamName}</h2><p className="lcc2-body text-sm">{team.ownerName}</p></div></div><div className="mt-4 rounded-lg border border-[var(--lcc-color-border)] p-3"><p className="lcc2-label">Outgoing package · {selected.length}/15</p>{selected.length ? <ul className="mt-2 space-y-2">{selected.map((id) => { const asset = catalog.find((candidate) => candidate.assetId === id); return asset ? <li key={id} className="flex items-center justify-between gap-2"><span className="min-w-0 truncate font-ui text-sm font-bold text-[var(--lcc-color-text)]">{asset.displayName} <span className="font-normal text-[var(--lcc-color-text-muted)]">{asset.marketValue !== undefined ? money(asset.marketValue) : ""}</span></span><button type="button" onClick={() => onRemove(id)} aria-label={`Remove ${asset.displayName} from ${label}`} className="lcc2-button lcc2-button--secondary px-2 py-1 text-xs">Remove</button></li> : null; })}</ul> : <p className="lcc2-body mt-2 text-sm">Select assets below.</p>}</div><DraftCapital picks={team.draftCapital} /></> : <p className="lcc2-body mt-4">Choose a franchise to view its roster and draft capital.</p>}</article>; }
function DraftCapital({ picks }: { picks: TradeAnalyzerTeam["draftCapital"] }) { const years = [...new Set(picks.map((pick) => pick.season))]; return <div className="mt-4 rounded-lg border border-[var(--lcc-color-border)] p-3"><p className="lcc2-label">Current draft capital</p>{years.length ? years.map((year) => <div key={year} className="mt-2"><p className="font-ui text-sm font-black text-[var(--lcc-color-text)]">{year}</p><p className="lcc2-body text-sm">{picks.filter((pick) => pick.season === year).map((pick) => `${pick.slot ? `${year} Pick ${pick.round}.${String(pick.slot).padStart(2, "0")}` : `${year} Round ${pick.round}`}${pick.originalTeamName !== "" && pick.currentOwnerId !== pick.originalTeamName ? "" : ""}`).join(" · ")}</p></div>) : <p className="lcc2-body mt-2 text-sm">No current picks.</p>}</div>; }
function RosterGroup({ position, assets, selected, add, teamA }: { position: string; assets: TradeAnalyzerCatalogAsset[]; selected: Set<string>; add: (id: string, side: "A" | "B") => void; teamA: string }) { return <div className="lcc2-card p-3"><h3 className="font-ui font-black text-[var(--lcc-color-text)]">{position}</h3>{assets.length ? <ul className="mt-2 space-y-2">{assets.map((asset) => <li key={asset.assetId} className="flex items-center gap-2 rounded-lg border border-[var(--lcc-color-border)] p-2"><div className="h-9 w-9 shrink-0 overflow-hidden rounded-full bg-[var(--lcc-color-surface-muted)]">{asset.imageUrl ? <img src={asset.imageUrl} alt="" className="h-full w-full object-cover" /> : <span className="flex h-full items-center justify-center text-xs font-bold">{asset.displayName.slice(0, 2)}</span>}</div><div className="min-w-0 flex-1"><p className="truncate font-ui text-sm font-bold text-[var(--lcc-color-text)]">{asset.displayName}</p><p className="truncate text-xs text-[var(--lcc-color-text-muted)]">{asset.nflTeam ?? "NFL team unavailable"}{asset.rosterStatus ? ` · ${asset.rosterStatus}` : ""}</p></div>{selected.has(asset.assetId) ? <span className="lcc2-badge lcc2-badge--info">Selected</span> : <button type="button" onClick={() => add(asset.assetId, asset.ownerId === teamA ? "A" : "B")} className="lcc2-button lcc2-button--secondary px-3 py-2 text-sm">Select</button>}</li>)}</ul> : <p className="lcc2-body mt-2 text-sm">No current assets.</p>}</div>; }
function AssetRow({ asset, selected, add }: { asset: TradeAnalyzerCatalogAsset; selected: boolean; add: (id: string, side: "A" | "B") => void }) { return <div className="rounded-lg border border-[var(--lcc-color-border)] p-3"><p className="truncate font-ui font-black text-[var(--lcc-color-text)]">{asset.displayName}</p><p className="lcc2-body text-sm">{asset.assetType === "PICK" ? "Draft pick" : asset.position ?? asset.assetType}{asset.ownerName ? ` · ${asset.ownerName}` : ""}</p><div className="mt-3 flex flex-wrap gap-2"><button type="button" disabled={selected} onClick={() => add(asset.assetId, "A")} className="lcc2-button lcc2-button--secondary px-3 py-2 text-sm disabled:opacity-50">{selected ? "Selected" : "Add to Side A"}</button><button type="button" disabled={selected} onClick={() => add(asset.assetId, "B")} className="lcc2-button lcc2-button--secondary px-3 py-2 text-sm disabled:opacity-50">{selected ? "Selected" : "Add to Side B"}</button></div></div>; }
function ResultView({ result, labels, catalog, snapshotDate }: { result: ApiData; labels: string[]; catalog: TradeAnalyzerCatalogAsset[]; snapshotDate: string }) { if (!result.trade || !result.sideA || !result.sideB || result.trade.resultStatus === "SUPPRESSED") return <section className="lcc2-card mt-5 p-5" aria-label="Trade result"><p className="lcc2-label">Fairness unavailable</p><h2 className="mt-2 font-ui text-2xl font-black text-[var(--lcc-color-text)]">Value unavailable for one or more assets.</h2><p className="lcc2-body mt-2">Current market value is unavailable for one or more selected assets.</p></section>; const side = (item: SideResult, index: number) => <div className="lcc2-card p-4"><p className="lcc2-label">{labels[index]}</p><p className="mt-2 font-ui text-2xl font-black text-[var(--lcc-color-text)]">{money(item.rawValue)}</p><p className="lcc2-body mt-1">{item.marketShare ? `${item.marketShare.display.toFixed(1)}% market share` : "Market share unavailable"}</p><p className="lcc2-body mt-1">Evidence · {item.evidence}</p><ul className="mt-3 space-y-1">{item.assets.map((asset) => <li key={asset.assetId} className="lcc2-body text-sm">{catalog.find((candidate) => candidate.assetId === asset.assetId)?.displayName ?? asset.displayName}{asset.baseValue !== undefined ? ` · ${money(asset.baseValue)}` : ""}</li>)}</ul></div>; return <section className="mt-5" aria-label="Trade result"><div className="lcc2-card lcc2-card--raised p-5 sm:p-6"><div className="flex flex-wrap items-end justify-between gap-3"><div><p className="lcc2-label">{result.trade.resultStatus === "PROVISIONAL" ? "Provisional" : "Authoritative"} result</p><h2 className="mt-2 font-ui text-4xl font-black text-[var(--lcc-color-text)]">{result.trade.fairnessScore ? `${result.trade.fairnessScore.display.toFixed(1)} / 100` : "Fairness unavailable"}</h2><p className="lcc2-body mt-1">{result.trade.fairnessBand ?? "Value unavailable for one or more assets."}</p></div><div className="text-left sm:text-right"><p className="lcc2-label">Market edge</p><p className="font-ui text-xl font-black text-[var(--lcc-color-text)]">{result.trade.marketEdgeSide === "NONE" ? "Even" : labels[result.trade.marketEdgeSide === "SIDE_A" ? 0 : 1]}</p><p className="lcc2-body text-sm">Evidence · {result.trade.evidence}</p></div></div><div className="mt-5 grid gap-4 md:grid-cols-2">{side(result.sideA, 0)}{side(result.sideB, 1)}</div><p className="lcc2-body mt-4 text-sm">Snapshot: {new Date(snapshotDate).toLocaleDateString("en-US", { month: "short", day: "numeric", year: "numeric", timeZone: "UTC" })}</p></div></section>; }
